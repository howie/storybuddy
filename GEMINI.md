# StoryBuddy

## Project Overview
StoryBuddy is a Python-based project currently in its initial setup phase. The codebase is currently sparse, containing primarily configuration for version control.

**Detected Technology Stack:**
*   **Language:** Python (Inferred from `.gitignore`)
*   **Status:** New Project / Scaffolding

## Development Conventions
*   **Environment:** It is expected that a virtual environment (e.g., `venv`, `pipenv`, `poetry`) will be used.
*   **Dependency Management:** Look for `requirements.txt`, `Pipfile`, or `pyproject.toml` in the future to handle dependencies.
*   **Testing:** Python projects typically use `pytest` or `unittest`.

## Building and Running
*   **Setup:**
    *   Create a virtual environment: `python3 -m venv venv`
    *   Activate it: `source venv/bin/activate` (macOS/Linux) or `venv\Scripts\activate` (Windows)
*   **Run:** *TODO: Add entry point execution command (e.g., `python main.py`)*
*   **Test:** *TODO: Add test command (e.g., `pytest`)*

## Directory Structure
*   `README.md`: Project documentation.
*   `.gitignore`: Specifies intentionally untracked files (configured for Python).
*   `GEMINI.md`: Contextual guide for Gemini AI agent interactions.

## Recent Changes
- 001-flutter-mobile-app: Added [if applicable, e.g., PostgreSQL, CoreData, files or N/A]
- 001-flutter-mobile-app: Added [if applicable, e.g., PostgreSQL, CoreData, files or N/A]
